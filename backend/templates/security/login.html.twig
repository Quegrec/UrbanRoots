{% extends 'base.html.twig' %}

{% block body %}
    <div class="flex justify-center items-center h-screen">
        <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
            <h1 class="text-2xl font-bold mb-6">Connection</h1>
            <form action="{{ path('app_login') }}" method="post">
                <input type="text" name="_username" class="mb-4 w-full p-2 border border-gray-300 rounded" required>
                <input type="password" name="_password" class="mb-4 w-full p-2 border border-gray-300 rounded" required>
                <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                    Se connecter
                </button>
            </form>
            <div class="text-center mt-4">
                <a href="" class="text-blue-500 hover:text-blue-700">Mot de passe oublié ?</a>
                <div>
                    <p>Si vous n'êtes pas encore inscrit, <a href="{{ path('app_register') }}" class="text-blue-500 hover:text-blue-700 underline">cliquez ici</a></p>
                    
                </div>
            </div>
        </div>
    </div>
{% endblock %}